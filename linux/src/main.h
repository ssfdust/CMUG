#define STRLEN 45
#define PASSWD 8
#define CHOICE 6
typedef struct nuser {
    char username[STRLEN];
    int school;
    char passwd[PASSWD];
} user;

/*
 * Collect user information
 * Username
 * Passwd
 * and school
 */
